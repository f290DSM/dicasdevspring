spring:
  application:
    name: dicasdevspring
  datasource:
    url: jdbc:h2:mem:fatec-db;DB_CLOSE_ON_EXIT=false
    username: aluno
    password: aluno@fatec
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  h2:
    console:
      enabled: true
      path: /h2-console

server:
  port: 8000